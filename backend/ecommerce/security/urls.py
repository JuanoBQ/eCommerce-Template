"""
URLs para el módulo de seguridad.
Define endpoints para validación y auditoría.
"""

from django.urls import path

app_name = 'security'

urlpatterns = [
    # Endpoints de seguridad (sin 2FA)
    # Aquí se pueden agregar otros endpoints de seguridad en el futuro
]
